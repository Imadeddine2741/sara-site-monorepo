spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/sara_site?useSSL=false&serverTimezone=UTC
    username: user                    # ✅ Cohérent
    password: zineb                   # ✅ Cohérent
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MariaDBDialect

jwt:
  secret: saraRahmaniSite2025SuperSecretKey123456789  # ← AJOUTE ÇA
  expiration: 86400000

server:
  port: 8080
